// Generated code from Icepick. Do not modify!
package com.cloversoft.ks.vendor.android.base;
import android.os.Bundle;
import icepick.Bundler;
import icepick.Injector.Helper;
import icepick.Injector.Object;

import java.util.Map;
import java.util.HashMap;

public class BaseActivity$$Icepick<T extends BaseActivity> extends Object<T> {

  private final static Map<String, Bundler<?>> BUNDLERS = new HashMap<String, Bundler<?>>();
  static {
        
  }

  private final static Helper H = new Helper("com.cloversoft.ks.vendor.android.base.BaseActivity$$Icepick.", BUNDLERS);

  public void restore(T target, Bundle state) {
    if (state == null) return;
    target.fragmentName = H.getString(state, "fragmentName");
    target.fragmentFrame = H.getInt(state, "fragmentFrame");
    super.restore(target, state);
  }

  public void save(T target, Bundle state) {
    super.save(target, state);
    H.putString(state, "fragmentName", target.fragmentName);
    H.putInt(state, "fragmentFrame", target.fragmentFrame);
  }
}